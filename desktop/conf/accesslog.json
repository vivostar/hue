{
  "version": 1,
  "disable_existing_loggers": false,
  "logging": {
    "formatters": {
      "default": {
        "format": "%(asctime)s %(levelname)-8s %(process)s %(name)s %(message)s"
      }
    },
    "handlers": {
      "file": {
        "class": "logging.handlers.RotatingFileHandler",
        "filename": "/var/log/hue/access.log",
        "maxBytes": 5000000,
        "backupCount": 5,
        "formatter": "default"
      }
    },
    "accesslog": {
      "level": "DEBUG",
      "handlers": ["file"]
    }
  }
}
